# 交给 vsce 打包时该忽略什么（我们自己的打包器走白名单，不受这个文件影响）。
#
# 默认会把整个仓库打进去——源码、测试、样例数据、SPEC 都进去了，包会大好几倍。
# 先排除一切，再把真正要发布的加回来。
**
!package.json
!dist/**
!media/**
!README.md
# 市场页面会把它渲染成「Changelog」标签页；我们的打包器早就装它，这里也得放行，
# 否则改用 vsce 自己打包时两边产物就会不一样。
!CHANGELOG.md
!LICENSE

# 注意两件事（2026-09-13 实测）：
#   1. 这个文件只能用来「放行」，别指望在这里排除 dist/ 里的东西——写 `dist/*.map`
#      这类排除，vsce 视而不见，照样把 sourcemap 与上一次的 VSIX 卷进包里。
#      用 vsce 打包前先清掉 dist/ 里的旧产物（见 PUBLISHING.md）。
#   2. 我们自己的打包器（src/tools/vsix.ts）不读这个文件，它按白名单收集，
#      并主动跳过 sourcemap 与 *.vsix（否则会把上一次的包打进这一次的包里）。
